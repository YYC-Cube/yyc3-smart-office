{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-unused-expressions": "off"
  },
  "overrides": [
    {
      "files": ["types/**/*.ts", "types/**/*.d.ts"],
      "rules": {
        "no-unused-expressions": "off"
      }
    }
  ]
}
